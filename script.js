document.addEventListener('DOMContentLoaded', () => {
    console.log('Flashiez Driving School website loaded successfully!');
});

// A simple example function you could use for future features
function greetUser() {
    const userName = prompt('What is your name?');
    if (userName) {
        alert(`Hello, ${userName}! Welcome to Flashiez Driving School.`);
    }
}

// You can uncomment the line below to test the function.
// greetUser();